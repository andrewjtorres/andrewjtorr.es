parameters:
  path: ''
  version: 'v1'
steps:
  - task: CacheBeta@0
    inputs:
      key: reports-${{ parameters.version }}-$(System.PullRequest.SourceBranch)
      path: ${{ parameters.path }}
    displayName: Cache reports
